import java.util.Scanner;
import java.util.Arrays;

public class Array {
    void minMaxFunction(int[] arr,int n){
        int n = arr.length;
        for(int i=o;i<n;i++){
            if(arr[i])
        }
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter size of Array");
        int s=sc.nextInt();
        int[] arr = new int[20];
        for(int i=0;i<arr.length;i++){
            arr[i] = sc.nextInt();
        }

        // int[] arr;
        // arr = new int[20];
        // Arrays.fill(arr, 10);

        // for (int i = 0; i < arr.length; i++) {
        //     System.out.print(arr[i] + " ");
        // }

        // So, when you write int min = INT_MAX;, you're initializing the variable min with the largest possible 
        // value that an integer can hold. This can be useful as a starting point minimum value in a series of integers.
        //  You can then compare other integers to this initial value to find the true minimum.

    }

}
